projection;
strict;
use draft;

define behavior for ZMB_C_CONN alias Connection
use etag

{
  use create;
  use update;
  use delete;

  use action Edit;
  use action Activate;
  use action Discard;
  use action Resume;
  use action Prepare;
}